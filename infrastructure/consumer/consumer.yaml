apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-consumer
  namespace: consumer-ns
  labels:
    app: kafka-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-consumer
  template:
    metadata:
      labels:
        app: kafka-consumer
    spec:
      containers:
        - name: kafka-consumer
          image: pmotwani575/kafka-consumer:latest
          env:
            - name: KAFKA_BROKER_URL
              value: "kafka-cluster.kafka-project.svc.cluster.local:9092"
            - name: KAFKA_TOPIC
              value: "test"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-consumer-service
  namespace: consumer-ns
spec:
  selector:
    app: kafka-consumer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
